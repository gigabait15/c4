# 📚 EGE Score Tracker

Приложение для учёта баллов ЕГЭ с FastAPI бэкендом и Telegram ботом.

## 📋 Описание

Проект состоит из двух компонентов:

- **FastAPI API** — REST API для управления пользователями и их баллами по предметам
- **Telegram Bot** — бот для удобного взаимодействия с системой через Telegram

### Функциональность

- 📝 Регистрация пользователей через Telegram
- 📚 Добавление баллов по предметам ЕГЭ (Математика, Русский язык, Физика, Химия и др.)
- 📊 Просмотр сохранённых баллов
- 🔒 Защита от дублирования предметов для одного пользователя

## 🏗️ Структура проекта

```
├── bot/                    # Telegram бот
│   ├── api_client.py       # HTTP клиент для API
│   ├── handlers.py         # Обработчики команд бота
│   ├── keyboards.py        # Клавиатуры бота
│   ├── main.py             # Точка входа бота
│   └── states.py           # FSM состояния
├── core/                   # Базовые модули
│   ├── base/
│   │   ├── config.py       # Конфигурация (pydantic-settings)
│   │   ├── crud.py         # Базовый CRUD класс
│   │   └── model.py        # Базовая модель SQLAlchemy
│   └── db/
│       └── session.py      # Сессия базы данных
├── src/                    # Основной код приложения
│   ├── api/                # API роутеры
│   │   ├── objects.py      # Эндпоинты для объектов (баллов)
│   │   └── users.py        # Эндпоинты для пользователей
│   └── app/                # Бизнес-логика
│       ├── objects/        # Модуль объектов
│       │   ├── crud.py
│       │   ├── model.py
│       │   ├── schema.py
│       │   └── service.py
│       └── users/          # Модуль пользователей
│           ├── crud.py
│           ├── model.py
│           └── schema.py
├── migrations/             # Миграции Alembic
├── tests/                  # Тесты
├── main.py                 # Точка входа FastAPI
├── alembic.ini             # Конфигурация Alembic
└── pyproject.toml          # Зависимости проекта
```

## 🛠️ Технологии

- **Python 3.12+**
- **FastAPI** — веб-фреймворк
- **SQLAlchemy 2.0** — ORM (async)
- **PostgreSQL** — база данных
- **Alembic** — миграции БД
- **Aiogram 3** — Telegram Bot API
- **Pydantic** — валидация данных
- **uv** — менеджер пакетов

## ⚙️ Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd C4Test
```

### 2. Установка зависимостей

```bash
# Установка uv (если не установлен)
curl -LsSf https://astral.sh/uv/install.sh | sh

# Установка зависимостей проекта
uv sync
```

### 3. Настройка окружения

Создайте файл `.env` в корне проекта:

```env
# База данных PostgreSQL
DB_NAME=ege_tracker
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432

# Telegram Bot
BOT_TOKEN=your_telegram_bot_token
```

### 4. Настройка базы данных

```bash
# Создайте базу данных PostgreSQL
createdb ege_tracker

# Применение миграций
uv run alembic upgrade head
```

## 🚀 Запуск

### 🐳 Запуск через Docker (рекомендуется)

```bash
# Скопируйте и настройте .env файл
cp .env.example .env
# Отредактируйте .env, добавив BOT_TOKEN и другие параметры

# Запуск всех сервисов
docker compose up -d

# Просмотр логов
docker compose logs -f

# Остановка
docker compose down

# Остановка с удалением данных БД
docker compose down -v
```

После запуска:
- API: http://localhost:8000
- Swagger UI: http://localhost:8000/docs
- PostgreSQL: localhost:5432

### Локальный запуск (без Docker)

#### Запуск API сервера

```bash
uv run python main.py
```

Или с автоматической перезагрузкой:

```bash
uv run uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

API будет доступен по адресу: http://localhost:8000

Документация API:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

### Запуск Telegram бота

```bash
uv run python -m bot.main
```

### Запуск обоих компонентов

Откройте два терминала:

**Терминал 1 (API):**
```bash
uv run python main.py
```

**Терминал 2 (Бот):**
```bash
uv run python -m bot.main
```

## 🧪 Тестирование

### Установка тестовых зависимостей

```bash
uv sync --extra test
```

### Запуск тестов

```bash
# Все тесты
uv run pytest

# С подробным выводом
uv run pytest -v

# Конкретный файл
uv run pytest tests/test_api.py -v

# Конкретный тест
uv run pytest tests/test_crud.py::TestUserCRUD::test_create_user -v
```

### Запуск с покрытием кода

```bash
# Покрытие в терминале
uv run pytest --cov --cov-report=term-missing

# HTML отчёт
uv run pytest --cov --cov-report=html
# Открыть htmlcov/index.html в браузере
```

### Структура тестов

| Файл | Описание |
|------|----------|
| `tests/conftest.py` | Фикстуры: async DB, TestClient, тестовые данные |
| `tests/test_schemas.py` | Валидация Pydantic схем |
| `tests/test_crud.py` | CRUD операции для User и Object |
| `tests/test_services.py` | Бизнес-логика (ObjectService) |
| `tests/test_api.py` | Интеграционные тесты API endpoints |
| `tests/test_api_client.py` | API клиент бота (моки HTTP) |

## 📡 API Endpoints

### Users

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `GET` | `/users/{user_id}` | Получить пользователя по ID |
| `GET` | `/users/telegram/{telegram_id}` | Получить пользователя по Telegram ID |
| `POST` | `/users/` | Создать пользователя |

### Objects (Баллы)

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `GET` | `/objects/{user_id}` | Получить все баллы пользователя |
| `POST` | `/objects/` | Добавить балл по предмету |

## 🤖 Команды Telegram бота

| Команда | Описание |
|---------|----------|
| `/start` | Начало работы с ботом |
| `/register` | Регистрация нового пользователя |
| `/select_subject` | Выбор предмета для добавления балла |
| `/view_scores` | Просмотр всех баллов |

## 📦 Миграции базы данных

```bash
# Создать новую миграцию
uv run alembic revision --autogenerate -m "описание изменений"

# Применить миграции
uv run alembic upgrade head

# Откатить последнюю миграцию
uv run alembic downgrade -1

# Посмотреть историю миграций
uv run alembic history
```

## 📄 Лицензия

MIT License

